# 01. Rubric traceability matrix

One row per confirmed sub-criterion, plus one row for engineering process, which the rubric does
not score directly and the judges ask about anyway.

**Rules for this table.** Every evidence cell is a path, a PR number or a test name. Never prose.
A red row is an open issue. Re-score the whole table at every milestone and fix the three lowest
rows first, not the easiest ones.

Status legend: **G** claim written and evidence exists, **Y** evidence partially exists,
**R** not built yet.

Last scored **2026-09-12 04:54 CST**, just past the M1 gate, against `dev` at `8487841`. The
evidence cells were refreshed **2026-09-12 05:30 CST** against `dev` at `4e9e2eb`, after PRs #129,
#131 and #133, and rows 3 and 7 again with issue #166, which added the payment rail and the
automatic cent. No status letter moved in either refresh: a refresh is not a re-score, and the next
one is M2.

**Consortium clauses added 2026-09-12 for issue #164, and no status letter moved.** Rows 1, 4 and 8
gained a clause about the cross-tenant beneficiary network of ADR-0006. The narrative evidence for
those clauses is in this pull request; the code evidence, `packages/consortium`, the three
`consortium:*` commands and `0009_consortium_snapshot.sql`, lands in the code pull request of the same
issue. So those three paths are the first cells to verify at the M3 re-score, and if the code does not
land, the clause comes out of the row rather than the row staying green on an intention.

| # | Sub-criterion (pts) | Our claim, one line | Evidence in this repo | Status |
|---|---|---|---|---|
| 1 | Substantiated competitive differentiation (10) | Every named competitor reads one of the three sources, and we are the only one that joins the CFDI ledger, the SAT 69-B list and the Banxico CEP while the payment can still be stopped. The fourth source is cross-tenant: a register of payee accounts pooled across customers is what the corporate payee-verification band already sells to treasuries, Eftsure's own material saying "over 6 million verified businesses", and nobody sells one to a Mexican PyME, while the two Mexican 69-B tools in the same table hold no account data at all and so could not build one | `docs/04-market.md#competitor-map`, `docs/00-challenge.md#prior-year-winners-we-are-deliberately-not-rebuilding`, `docs/adr/0002-track-and-thesis.md`, `docs/adr/0006-consortium-snowflake.md#what-the-incumbents-actually-sell-read-on-their-own-pages` (PR #23, PR #93, #164) | G |
| 2 | Identification of market gap (10) | The three facts that decide whether a payment is safe are all public and all current, they belong to three different tools and three different people, and nothing sits in the few minutes between approving a payment run and sending it | `docs/04-market.md#the-gap`, `docs/04-market.md#problem-sizing` (PR #93) | G |
| 3 | Non trivial solution (10) | Six explainable controls over one event log: the 3-7-1 check digit plus OCR-aware Damerau-Levenshtein against the accounts this supplier was actually paid on, the plaza in digits 4 to 6 resolved to a place and compared both against the plazas the supplier has been paid in and against the state of the CFDI `LugarExpedicion`, which is a comparison neither document can make alone because the plaza lives in the account number and the place lives in the invoice, XMLDSig verification of a Banxico seal, a retroactive sweep that is a fold over `LedgerEvent[]` with no clock and no network, and the beneficiary control run end to end from one press: the 0.01 MXN probe leaves through a payment rail, the clave de rastreo comes back from the bank rather than from a keyboard, and the expected-loss rule releases or blocks the payment when the CEP arrives. On top of them three levels and three states instead of a probability, derived in one pure function with a test per row of the ADR-0009 table, where a definitive SAT listing cancels the line with a ledger event and only a named owner reopens it with a written reason | `packages/core/src/clabe.ts`, `packages/cep/src/signature.ts`, `packages/sat/src/sweep.ts`, `packages/engine/src/index.ts`, `packages/rail/src/`, `packages/core/src/levels.ts`, `packages/core/src/levels.test.ts`, `apps/api/src/verification.ts`, `apps/api/src/verification.test.ts` tests `releases the held payment when the CEP names the supplier` and `blocks the payment when the CEP names somebody else` (PR #117, PR #119, issues #166 and #204) | G |
| 4 | Data foundation (6) | One synthetic company, eight months of CFDI and payment complements plus a Nessie-shaped bank mirror that reconciles to the cent, deterministic from one seed, with every account opened in the plaza of the municipality that invoices from it; a committed 786-row plaza catalogue whose README states in its first paragraph that it is NOT Banxico's file, carries the five repeatable checks that establish Banxico and the ABM publish no such file, names the SPEI participant that does, and is therefore allowed to put a name on three digits and nothing else, which is why the plaza comparison is arithmetic over our own ledger and the catalogue is read only to write the sentence; the real 4.5 MB SAT 69-B listing committed with its provenance, next to the honest coverage of the second SAT list, article 49 Bis, whose loader, thirty day window and sweep are written and whose listing the SAT publishes as fourteen separate DOF oficios and no file, so the lookup answers `answered: false` with the counts and the URL instead of implying a check; 35 labelled holdout cases, 12 of them hard negatives, whose labels come from ADR-0002 and the domain types rather than from the control source, each carrying an expected confidence level as well as its expected findings; and the parser proven on three real CFDI 4.0 de ingreso received on 2026-09-12, amounts scaled by a factor that is not in the repository and every identifier synthetic, each asserted to parse through the production path and none of the parser's tolerances; plus a fourth data source that is cross-tenant rather than ours, a Snowflake network of beneficiary outcomes holding only salted hashes of a supplier and an account, a bank code, an outcome and a date, whose other tenants are generated deterministically from the same seed 69 and written with `synthetic = true`, seeded, pushed and pulled against a real Snowflake account on 2026-09-12 and landing 46 hashed pairs in the local snapshot, which is also fillable with no account at all so the decision reads it offline | `packages/seed/src/sentryone/`, test `reconciles to the cent against the complements and the transfers`, `packages/sat/src/snapshot/README.md`, test `reconciles: 14234 rows, 91 of them unreadable, 28935 situations`, `packages/sat/src/art49bis.test.ts` test `says the list is not loaded, and why, with the counts to check it`, `packages/seed/src/holdout/cases/`, `packages/seed/src/holdout/README.md`, `packages/core/src/fixtures/real/`, `scripts/import-real-cfdi.ts`, `packages/core/src/cfdi-real.test.ts` test `needed none of the parser's tolerances to be read`, `docs/08-data-model.md#real-document-validation`, `packages/consortium`, `packages/db/migrations/0009_consortium_snapshot.sql`, `bun run consortium:seed` and `bun run consortium:pull`, `docs/adr/0006-consortium-snowflake.md` (PR #119, PR #122, PR #123, PR #159, #164, #180) | G |
| 5 | Algorithmic logic and intelligence (9) | All six controls run over one typed `ComposeInput` and every one lands in `ran` or `skipped` with a named reason, and the same entry point scores 35 labelled cases at 87.0 percent precision, 83.3 percent recall and a 1.6 percent false positive rate, read again per confidence level where 12 of 12 lines that should be confiable are, with the four labels that disagree with the engine left in the table rather than edited out | `bun run eval`, `GET /api/v1/metrics`, `packages/seed/src/holdout/engine.ts`, `packages/engine/src/engine.test.ts` test `accounts for all six whatever the evidence, and never loses one`, `apps/api/src/routes/instructions.test.ts` test `accounts for every one of the six controls, with no silent slot`, and the same controls over the seeded payment run in `apps/api/src/assess.ts` (PR #117, PR #122, PR #133) | G |
| 6 | System design (5) | One runtime, one SQL dialect, an explicit dependency direction (`packages/engine` exists so that `packages/core` never imports `sat` or `cep`), every choice recorded as an ADR, and an offline fallback that is the same numbers rather than a degraded mode: `supplier_weekly_outflow` is one name over a plain view and a Timescale continuous aggregate, compared column by column without a database and shown to reach the same 45,003,735.01 pesos through the event ledger and through the projection table | `docs/07-architecture.md`, `docs/adr/`, `packages/engine/README.md`, `docs/08-data-model.md#supplier_weekly_outflow-one-name-over-two-definitions`, `packages/db/src/migrate.test.ts` test `answers with the same five columns in the same order`, `packages/db/src/queries.test.ts` test `totals the same pesos the cfdis table holds, to the cent`, `docs/07-architecture.md#deploy-topology-and-commands`, `deploy/`, `apps/api/Dockerfile`, `vercel.json` (PR #117, PR #154) | Y |
| 7 | Quality and functional demo (5) | `bun run demo` drives the five beats of `docs/10-demo-script.md` headless against a freshly seeded in-memory API as nine checks, the four extra ones being the CEP parser on its own, the consortium network inside a decision offline, the level and the state of every line of the run with the one-page letter of the line a publication cancelled, and the payment run leaving on the rail, exits non-zero on any of them and prints the hero ids, 2,196 tests across 118 files pass with no network, no database and no key (the 118 database cases skip unless `TEST_DATABASE_URL` names one, and they share one, so they are run a workspace at a time), and each of the five integrations that leave this repository has been run against the real provider: two outbound verification calls, one image through Gemini, a Nessie write, the 0.01 MXN verification probe on the company's Nessie mirror, and one whole payment run of 86 lines for 1,388,920.90 MXN executed on that same mirror on 2026-09-13 with the counts in `packages/rail/README.md` | `scripts/demo.ts`, `docs/10-demo-script.md#numbers-the-screen-shows`, `apps/api/src/sentryone.test.ts`, `.github/workflows/ci.yml`, `docs/14-process.md#live-integrations-verified` with conversation ids `conv_6401m2ah87gnffctr757c34b5mdg` and `conv_2301m2ah9vnee2h8d14gpf1rb3rz`, and the deployed pair a judge can open on their own phone, `https://sentryone-one.vercel.app` over `https://api.104.238.147.69.sslip.io`, smoke tested by `scripts/deploy-vultr.ts`, `packages/rail/README.md#verified-against-nessie-on-2026-09-13-the-payment-run`, `apps/api/src/routes/execute.test.ts` (PR #133, #198) | Y |
| 8 | Substantiated business model (10) | MXN 899 per company per month, anchored against two published competitor prices and one published wage floor, breaking even at one stopped invoice of MXN 23,452 of subtotal per year, and one mechanism that compounds rather than a flat subscription: every tenant that joins the consortium improves the signal for every other tenant, an accounting firm holding thirty companies joins thirty tenants in one sale, and the marginal cost of the network is credits per payment run rather than per peso moved | `docs/05-business-model.md#who-pays-and-why-that-number`, `docs/05-business-model.md#unit-economics`, `docs/05-business-model.md#gtm-who-sells-this-to-whom-and-through-which-channel`, `docs/adr/0006-consortium-snowflake.md#consequences`, `docs/06-regulatory-privacy.md#86-cost-per-pull` (PR #93, #164) | G |
| 9 | Market size, TAM/SAM/SOM (5) | Bottom-up only, entities times price, every input cited to INEGI CE 2024 or to the SAT open-data file a judge can download and count themselves | `docs/04-market.md#sizing`, `docs/04-market.md#sources` (PR #93) | G |
| 10 | Regulatory and operational feasibility (5) | Payer-side software, not a regulated entity, with the framework map, the verified text of CFF 69-B, 69-B Bis and 49 Bis read out of the compiled statute on the day of the build, including the thirty natural days and the seal restriction that article 49 Bis puts on our own user, the LLM boundary enforced by a test that reads the package's own source, and the one thing the demo could be mistaken for stated rather than implied: the identity selector is not authentication, the `X-Actor` header is caller-controlled, and the page lists what production needs instead | `docs/06-regulatory-privacy.md#1-our-legal-position`, `docs/06-regulatory-privacy.md#2-framework-map-mexico`, `docs/06-regulatory-privacy.md#33-article-49-bis-the-newest-list-and-the-clock-that-points-at-our-own-user`, `packages/extract/src/boundary.test.ts` test `names nothing from the decision layer in its code`, `docs/06-regulatory-privacy.md#44-the-identity-selector-is-a-demo-affordance-not-authentication`, `apps/api/src/middleware/actor.ts` (PR #87, PR #116, #180, #199) | G |
| 11 | Adoption strategy, GTM (5) | Named segment counted to 6,476 establishments in three sectors in Nuevo Leon and about 2,312 after a formality rate we say is too low, two named founders selling in months 1 to 6 to purchasing and finance rather than to the clerk who operates it, behind a free 200-sweep exposure report with the conversation arithmetic written out, accounting firms as resellers from month 6 against a counted denominator of 143 in-band despachos of the state's 737, ERP and PAC integrations gated on ten paying firms, a bank embedding the control where the payment executes as the third route with the intermediation dilemma answered, and every one of those stated as a route nobody has agreed to rather than as a deal | `docs/05-business-model.md#gtm-who-sells-this-to-whom-and-through-which-channel`, `docs/05-business-model.md#the-third-route-a-bank-embeds-the-control-where-the-payment-executes`, `docs/04-market.md#where-the-segment-actually-is`, `docs/12-judge-qa.md#second-table-of-12-september` (PR #93, #193) | G |
| 12 | Specific user persona (7) | One named quantified composite, Lupita Elizondo, plus a corporate-treasury anti-persona, with the two venue interviews carried as open tasks rather than invented quotes | `docs/02-persona.md`, `docs/02-persona.md#pending-human-validation`, `assets/persona/lupita-elizondo.png` (PR #114) | Y |
| 13 | Structured user journey map (7) | Six stages from XML receipt to archived evidence, each mapped to the screen and the ledger event that carry it, with three human-decision branches including the false positive, and every one of those screens designed in four states rather than a happy path | `docs/03-user-journey.md#stage-by-stage-map`, `apps/web/src/screens/`, `apps/web/src/screens/states.test.ts`, and the stage where the payment actually leaves in `apps/web/src/screens/PaymentsScreen.tsx` with the two tables of issue #212, one for what the run hands the rail and one for what it does not take, each line carrying the reason (PR #114, PR #115, PR #124, PR #127) | Y |
| 14 | Pitch (6) | Three timed variants in Mexican Spanish opening with the fiscal hook cited to CFF 69-B and the Ley de Sistemas de Pagos, the eight hardest questions answered in one breath each, a gate table of which sentences may be spoken today, where the live verification call is ticked against two real outbound calls and the two rows still unticked carry the sentence to say instead, a table of the only numbers we are allowed to say with the source of each, a per-person answer sheet and a printable A5 judge card with a repository QR that was decoded before it was committed, and the six objections three Capital One judges actually raised on 2026-09-12 answered in thirty seconds each, with the value stated in pesos prevented instead of minutes saved and the competition answered by naming the nine Mexican and foreign entries `docs/04` documents rather than claiming an empty window, plus the one question a second Capital One panel asked that evening answered in counts rather than categories: the market sentence at 3:25 narrowed to the 6,476 of the segment before the national figure, and the 3:45 ask turned into the channel ask, three despacho introductions and one business-banking conversation | `docs/11-pitch.md#what-is-true-right-now-and-what-is-gated`, `docs/11-pitch.md#the-value-is-the-loss-not-the-minutes`, `docs/12-judge-qa.md#table-feedback-of-12-september-and-the-answers`, `docs/12-judge-qa.md#second-table-of-12-september`, `docs/13-devpost.md`, `docs/print/judge-card.html`, `docs/print/team-card.html`, `docs/print/README.md` (PR #98, PR #120, PR #131, #171, #193) | Y |
| plus | Engineering process (not scored directly) | 45 merged PRs on a board with nine views, 42 into `dev` and 3 release PRs into `main` with zero direct pushes to either, every PR body naming what it deliberately did not do, five ADRs, and a written cut list where one cut is recorded together with the PR that reversed it | `docs/14-process.md`, `docs/14-process.md#3-deliberate-descopes-each-recorded-in-the-pr-that-made-the-call`, `docs/adr/`, the project board, PR #117, PR #118, PR #119 | Y |

## Why the yellow rows are yellow

Five of the fourteen scored rows, plus the process row. Each one names the single thing that would
turn it green. Nothing else belongs in this list.

| # | What is missing | Tracked in |
|---|---|---|
| 6 | ADR-0003 and ADR-0004 still read `Proposed`. The two-host topology ADR-0005 decided is deployed as of 2026-09-12 15:32 CST, web on Vercel and API on Vultr against Tiger Data | The two ADR statuses have no issue of their own, which is itself the thing to fix |
| 7 | The live URL exists and answers, but no rehearsal has been run against it and `bun run demo --base <url>` has not been driven over it, because that beat appends one instruction to the live run and the documented totals would move | #73 for the rehearsal |
| 12 | The two venue conversations in `docs/02-persona.md#pending-human-validation` have not happened, so the workload numbers are synthetic and are labelled as such | #52, closed with those two acceptance criteria deliberately left unchecked rather than ticked |
| 13 | The same two conversations, plus the constancia PDF that stage 6 of the journey names | #69 |
| 14 | No rehearsal has been run and no video exists. The market, model and regulation block of the pitch now carries cited numbers instead of the `TODO(FabriBanda)` the slide table used to carry | #73, #75 |
| plus | Build night mode has the approval gate off, so no merged PR carries a post-merge review thread yet. The PR bodies carry the reasoning in the meantime | `docs/14-process.md` |

## Self-score

Under one stated rule, so that the number is reproducible rather than a feeling: **G scores the
full points, Y scores half, R scores zero.** M1, 2026-09-12 04:54 CST:

| Criterion | Available | Ours |
|---|---|---|
| Originality | 30 | 30 |
| Technical Depth | 25 | 20 |
| Impact and Feasibility | 25 | 25 |
| Design and Experience | 20 | 10 |
| **Total** | **100** | **85** |

Read it as what it is: our own score, computed from our own status letters. Its only real use is
the trend and the ranking of what to fix next, which right now is Design and Experience, where ten
of the twenty available points are sitting behind two conversations, one rehearsal and one video,
and none of them behind a line of code.

## How to use this at a walk-up

An engineer asking "what is actually real right now" is asking for column four. Open this file,
read the row, open the path. That is the whole answer and it takes fifteen seconds.

## Scoring discipline

- A row is only **G** when the evidence path exists on `dev` and the claim sentence is written.
  An empty claim with a real file is still **R**, because the judge needs the sentence. `dev` is
  the integration branch, so it is the honest place to check; `main` only receives release PRs.
- Score at M1, M2, M3, M4. Four data points is enough to see which criterion is being neglected.
- Link this file from the README. The matrix itself reads as unusual rigor, independently of the
  scores in it.
- Record every re-score in `docs/14-process.md` under the rubric score trend, so the shape of the
  36 hours is visible and not just the final number.
