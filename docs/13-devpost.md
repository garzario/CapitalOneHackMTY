# 13. Devpost submission copy

Written at M3 while awake. Pasted at M5. Submission goes to hackmty-26.devpost.com.

**A draft is submitted at M4 and edited until the deadline.** That is the cheapest disaster insurance
available and it removes the 07:50 panic entirely.

Owner: Fabricio (`FabriBanda`). Due M3, submitted as a draft at M4.

## Tagline

One line, under 80 characters, no buzzwords. TODO(FabriBanda)

## Elevator pitch, the short field

Two or three sentences. The persona, the moment, the mechanism. TODO(FabriBanda)

## The problem it solves

TODO(FabriBanda). Lift the opening of `docs/04-market.md` and the trigger moment from
`docs/02-persona.md`. One cited number.

## What it does

TODO(FabriBanda). Three to five bullets, each an outcome rather than a feature.

## How we built it

TODO(FabriBanda). Name the stack and tie each choice to the problem in one clause, because fit for
purpose is graded. Pull from `docs/07-architecture.md`. Mention that the intelligence is pure,
dependency-free, unit-tested TypeScript and that the data is deterministic synthetic.

## Challenges we ran into

TODO(FabriBanda). Real ones, with the resolution. The verified Nessie quirks are a genuine and
specific answer: dates with no time component, inconsistent empty sub-collections, mixed numeric
types, and a shared enterprise pool that cannot be used for analytics.

## What we learned

TODO(FabriBanda). One technical, one product, one about working as four people across 36 hours.

## What is next

TODO(FabriBanda). Three bullets from the twelve-month section of `docs/05-business-model.md`.

## Built with

TODO(FabriBanda). The tag list. Keep it honest and short: bun, typescript, hono, postgres,
timescaledb, react, vite, tailwindcss, plus the Nessie API if it is in the final build.

## Prize categories to select

- [ ] Capital One challenge, track TODO(garzario)
- [ ] Best Use of Tiger Data, only if the hypertable and the continuous aggregate are in the final
      build and the usage is genuine
- [ ] TODO(fabbyyyy): any other MLH category only if an ADR at M2 said yes

Never select a category we did not genuinely use. A category claim a judge can falsify costs more
than the prize is worth.

## Links and attachments

| Field | Value |
|---|---|
| Repository | https://github.com/garzario/CapitalOneHackMTY |
| Live URL | TODO(fabbyyyy) |
| Demo video | TODO(FabriBanda), hosted per the event's stated requirement |
| Screenshots | Three stills from `assets/screenshots/` |

## Team members to add

| Name | Devpost handle |
|---|---|
| Patricio Garza | TODO(garzario) |
| Fabian | TODO(fabbyyyy) |
| Adan | TODO(Apanawa) |
| Fabricio | TODO(FabriBanda) |

## Submission checklist

- [ ] Draft submitted at M4, not at M5
- [ ] All four teammates added and they confirmed they can see it
- [ ] Video plays from the link in a private browser window
- [ ] Live URL loads on a phone on cellular data
- [ ] Repository is public
- [ ] Screenshot of the submission confirmation saved
